# ============================================
# DeepTutor Python Dependencies
# ============================================
# Python >= 3.10 required
# Install: pip install -r requirements.txt
# Versions pinned from conda env (2026-02-28)

# ============================================
# Core dependencies
# ============================================
python-dotenv==1.2.2
PyYAML==6.0.3
tiktoken==0.12.0
jinja2==3.1.6              # Template engine for search consolidation

# ============================================
# HTTP and API clients
# ============================================
requests==2.32.5
openai==1.109.1
anthropic==0.84.0          # Anthropic Claude API
perplexityai==0.30.1
dashscope==1.25.12
aiohttp==3.13.3
httpx==0.28.1
urllib3==2.6.3

# ============================================
# Async support
# ============================================
nest-asyncio==1.6.0
tenacity==8.5.0

# ============================================
# Web framework and server
# ============================================
fastapi==0.135.1
uvicorn[standard]==0.41.0
websockets==15.0.1
python-multipart==0.0.22
pydantic==2.12.5

# ============================================
# RAG and knowledge base
# ============================================
raganything==1.2.9

# ============================================
# LlamaIndex
# ============================================
llama-index==0.14.15

# ============================================
# Academic and research tools
# ============================================
arxiv==2.4.0

# ============================================
# Scientific computing (for RAG and code execution)
# ============================================
numpy==1.26.4              # NumPy <2.0 for compatibility
# matplotlib==3.10.8       # Uncomment if needed for plotting
# scipy==1.15.3            # Uncomment if needed for scientific computing
# pandas==2.3.3            # Uncomment if needed for data analysis

# PDF parsing
PyMuPDF==1.27.1            # For PDF text extraction (fast and reliable)

# Vector search (optional but recommended for llamaindex pipeline)
# faiss-cpu>=1.7.4         # Uncomment for CPU-only FAISS support (faster search)
# faiss-gpu>=1.7.4         # Uncomment if you have GPU and want GPU acceleration

# ============================================
# Development and testing tools (dev only, not needed in production)
# Install separately: pip install pytest pre-commit safety bandit
# ============================================
# pytest==8.4.2
# pre-commit==4.5.1
# safety==2.3.5
# bandit==1.9.2

# ============================================
# LangChain LLM Provider
# ============================================
langchain-core==0.3.83         # Core LangChain abstractions
langchain-openai==0.3.0        # OpenAI integration (ChatOpenAI)
langchain-anthropic==0.3.22    # Anthropic integration (ChatAnthropic)
langchain-community==0.4.1    # Community integrations (SQLiteCache)
langchain-ollama==0.3.10       # Ollama integration (ChatOllama)

# ============================================
# LangGraph (workflow orchestration)
# ============================================
langgraph==1.0.10
langgraph-checkpoint-sqlite==2.0.0

# ============================================
# Database / Storage (Optional)
# ============================================
psycopg2-binary==2.9.11
passlib[argon2]==1.7.4
PyJWT==2.11.0
email-validator==2.3.0
